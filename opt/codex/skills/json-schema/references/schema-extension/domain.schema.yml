# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
# derived from: ./domain.schema.example.yml
---
type: object
additionalProperties: false
required:
  - adoptions
properties:
  adoptions:
    type: array
    items:
      type: object
      # reuse the base schema's subschemas using `allOf`
      allOf:
        - $ref: ../domain.schema.yml#/$defs/audit

        - type: object
          required:
            - dog-name
            - adopter
          properties:
            dog-name:
              type: string
            adopter:
              type: string
      unevaluatedProperties: false
