# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
# derived from: ./domain.schema.example.yml
---
# top level should always be object to support schema merging
type: object

# default to closed set of object properties
additionalProperties: false

# allow docker-compose.yml style explicit extension properties
patternProperties:
  ^x-: {}

# default to making all properties required
required:
  - dogs

# yml anchors should be extracted into $defs
$defs:
  audit:
    type: object
    required:
      - updated-at
    properties:
      updated-at:
        type: string
        # derive description from `description:` comments
        description: jq compatible timestamp
        pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"

properties:
  dogs:
    type: array
    items:
      type: object
      allOf:
        - $ref: "#/$defs/audit"
        - type: object
          required:
            - name
            - breed
          properties:
            name:
              type: string
            breed:
              enum:
                - wiener
                - schnauzer
                - pomeranian
                - poodle-shaped
      unevaluatedProperties: false
  locations:
    type: array
    items:
      type: object
      allOf:
        - $ref: "#/$defs/audit"
        - type: object
          required:
            - home
          properties:
            home:
              type: string
      unevaluatedProperties: false
