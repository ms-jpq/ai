(version 1)
(deny default)
(debug deny)

(import (string-append (param "SANDBOX") "/base.sb"))
(import (string-append (param "SANDBOX") "/auth.sb"))
(import (string-append (param "SANDBOX") "/git.sb"))

;; coding-agent.sb
;; Invoke:
;;   sandbox-exec \
;;     -f coding-agent.sb \
;;     -D SANDBOX="$PWD/sandbox/darwin" \
;;     -D HOME="$HOME" \
;;     -D CWD="$PWD" \
;;     -D TMPDIR="${TMPDIR%/}" \
;;     -D SSH_AUTH_SOCK="$SSH_AUTH_SOCK" \
;;     -- <command> [args...]
