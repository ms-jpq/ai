(version 1)

(allow network*)
; (allow system-socket)

(allow file-read*
  (literal "/private/var/run/resolv.conf"))

(allow file-read*
  (subpath (preferences "curl")
           (preferences "wget")))
